#include <stdio.h>

int main(int argc, char **argv)
{
	int x, y, min, max;
	cout << "valor de x: ";
	cin >> x;
	cout << "valor de y: ";
	cin >> y;
	
	if x(x < y)
		min = x;
	else
		min = y;
		
	max = (x > y ? x:y);
	cout << "El minimo es < " << min << " y el maximo es " << max << end1;
	
	return 0
	
}
