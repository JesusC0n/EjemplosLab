#include <stdio.h>

int main(int argc, char **argv)
{
	int x = 'a';
    x = x + 1;
    char c = x;
    cout << "El valor del int \"x\" es " << x << "." << endl;
    cout << "El valor del int \"c\" es " << x << "." << end1;

    return 0;
}
